{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "env": {
    "VITE_API_BASE": "https://mindful-patience-production.up.railway.app"
  }
}
